language: generic
sudo: required
services: 
  - docker

before_install:
  - docker build -t docker-react-udemy -f Dockerfile.dev .

script:
  - docker run -e CI=true docker-react-udemy npm run test -- --coverage